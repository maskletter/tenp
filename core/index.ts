
import { dbRouterInfo, Router, Config, Get, Post, Delete, Put, Head } from './lib/router'
import Start from './lib/server'
export { 
	RouterConfig, 
	ConfigInterface, 
	RouterInfo,
	PathInfo,
	StartInterface,
	GetInterface, PostInterface, DeleteInterface, PutInterface, HeadInterface,
	Request, Response } from './d.ts/interface'


function tenp(){
	
}

export { Start, Router, Config, Get, Post, Delete, Put, Head };
export default tenp;
